{
  "ignoreCommand": "if [[ \"$VERCEL_GIT_COMMIT_REF\" == \"main\" || \"$VERCEL_GIT_COMMIT_REF\" == \"dev\" ]]; then exit 1; else exit 0; fi",
  "buildCommand": "NEXT_PUBLIC_ROLLBAR_CODE_VERSION=$VERCEL_GIT_COMMIT_SHA yarn build && node scripts/upload-sourcemaps.cjs",
  "crons": [
    {
      "path": "/api/cleanup-orphan-files",
      "schedule": "0 2 * * 0"
    }
  ]
}
